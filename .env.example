# Copy this file to .env and fill in your values.
# .env is gitignored and never pushed to GitHub.

# Your Cloudflare Worker domain or workers.dev subdomain
WORKER_DOMAIN=your-worker.workers.dev

# Cloudflare Pages project name (used by `make example-deploy`)
PAGES_PROJECT=zrtc-demo
